<h1>403 - Access Denied</h1>

<p>You are logged in, but you do not have the Admin role.</p>

<p>
  <strong>Your roles:</strong>
  <%= user.roles && user.roles.length ? user.roles.join(', ') : 'No roles claim received' %>
</p>

<p>
  <strong>Your groups:</strong>
  <%= user.groups && user.groups.length ? user.groups.join(', ') : 'No groups claim received' %>
</p>

<p><a href="/profile">Back to Profile</a></p>
